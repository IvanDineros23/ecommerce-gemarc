<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class CreateDemoProductsSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // Minimal stub seeder for demo products.
        // The original project may have added demo products here.
        // Keep this minimal to avoid accidental data issues during local setup.
        // If you want demo products, we can add a few sample insertions later.
    }
}
